const { extractKeysFromP12 } = require('./dist/lib/siat/crypto/signer.js'); // Next.js alias wont work in plain node
// Since TS alias won't work in plain node, let's write a pure node test script to test node-forge.
